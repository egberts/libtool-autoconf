
AUTOMAKE_OPTIONS = foreign

lib_LTLIBRARIES = libmethod2d.la  # creates .so

libmethod2d_la_SOURCES = method2.c

# libmethod2d.a library ends up in ./.libs subdirectory
# libmethod2d.a does get installed via `make install`.
# libmethod2d.a does get packaged via `make dist`.
